name: Test Project
on: 
  push:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: telling hai
        run: |
             echo"delated happy birthday anuhya"
             echo"where is the party"
        

